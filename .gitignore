# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# runtime data (lowdb store is generated at runtime)
/data/*.json

# static snapshot: tracked on purpose. The static frontend reads it from GitHub's
# raw CDN, so it must live on the default branch; the cron workflow refreshes it.

# misc
.DS_Store
*.pem
.vscode/
.idea/

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# typescript
*.tsbuildinfo

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
